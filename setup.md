navigate to your chosen location to clone your repo using a terminal

clone the repo
```bash
git clone https://github.com/Jack-Lean-1404/Frontlines.git
cd Frontlines
```
install the requirements
```bash
pip install -r requirements.txt
```

install flask
```bash
pip install flask
```


